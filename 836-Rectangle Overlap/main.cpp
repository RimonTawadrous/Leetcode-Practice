#include <bits/stdc++.h>
using namespace std;

// #define x1 0
// #define x2 2
// #define y1 1
// #define y2 3

// class Solution
// {
// private:
//     bool isPointInRectangle(vector<int> &point, vector<int> &rec)
//     {
//         bool isInX = false, isInY = false;
//         if ((point[0] > rec[x1] && point[0] < rec[x2]) || (point[0] < rec[x1] && point[0] > rec[x2]))
//         {
//             isInX = true;
//         }
//         if ((point[1] > rec[y1] && point[1] < rec[y2]) || (point[1] < rec[y1] && point[1] > rec[y2]))
//         {
//             isInY = true;
//         }

//         return isInX && isInY;
//     }

// public:
//     bool isRectangleOverlap(vector<int> &rec1, vector<int> &rec2)
//     {
//         vector<int> p1 = {};
//         return isPointInRectangle((vector<int>){rec2[x1], rec2[y1]}, rec1) ||
//                isPointInRectangle((vector<int>){rec2[x2], rec2[y2]}, rec1) ||
//                isPointInRectangle((vector<int>){rec2[x1], rec2[y2]}, rec1) ||
//                isPointInRectangle((vector<int>){rec2[x2], rec2[y1]}, rec1);
//     }
// };

int main()
{
}